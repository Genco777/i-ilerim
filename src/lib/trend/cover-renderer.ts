/**
 * Deprecated: the real implementation lives in cover-renderer.tsx (V-7).
 *
 * This file remains only because the sandbox could not delete it; importers
 * should reference './cover-renderer.tsx' explicitly. Nothing useful is
 * exported here.
 */
export {};
