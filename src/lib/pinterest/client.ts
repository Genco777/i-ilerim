/**
 * @deprecated
 *
 * Bu dosya yanlışlıkla oluşturuldu — Pinterest OAuth/API client zaten
 * `src/lib/publish/pinterest.ts` içinde implement edilmiş. Yeni kod o modülü
 * kullanmalı:
 *
 *   import {
 *     buildAuthorizeUrl,
 *     exchangeCodeForToken,
 *     persistTokens,
 *     isPinterestConnected,
 *     listBoards,
 *     pickBoardsForNiche,
 *     createPin,
 *   } from '@/lib/publish/pinterest';
 *
 * Bu dosya manuel silinene kadar boş bırakıldı (Windows mount sandbox'tan
 * silemedi). Mehmet: `git rm src/lib/pinterest/client.ts src/lib/pinterest/publish.ts`
 */
export {};
